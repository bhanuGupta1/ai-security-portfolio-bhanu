# Core — zero external dependencies for rule-based detection
# Python 3.10+ standard library only: re, unicodedata, dataclasses, enum, logging, hashlib, json, argparse

# ── Development ──────────────────────────────────────────────────
pytest>=7.0.0
pytest-cov>=4.0.0

# ── API / Web frontend ───────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.20.0

# ── Optional: Claude / Anthropic LLM integration ─────────────────
# anthropic>=0.40.0

# ── Optional: embedding-based detection (future) ─────────────────
# sentence-transformers>=2.2.0
# torch>=2.0.0
